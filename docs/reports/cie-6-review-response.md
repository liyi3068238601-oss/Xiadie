# CIE.6 独立 Review 处置

- Review：`E:\Xiadie\review\cie6-review\cie6-review.html`
- 结论：通过，0 P0 / 0 P1，2 P2，允许 CIE v1 正式冻结。
- 日期：2026-07-29

## 处置

1. **P2-1 延后：周期清理异常残留图片。** 当前正常发送后立即销毁原始图片，启动与新上传前也会清理过期文件；剩余风险只涉及应用长期运行且此前发生异常退出的残留。把清理直接塞入聊天或投递轮询会混合所有权并引入新的 I/O 时序，后续应在统一维护调度中实现有界周期 GC、互斥与失败诊断。
2. **P2-2 延后：精简回放观察器元数据。** `affect_observation` 与 `memory_observation` 是 5 分钟 TTL 内的结构化用户可见状态，不含第三方正文或完整推理；现有前端重放可能依赖它们恢复最终状态。冻结前删除会改变 `cie-cancel-control-v1` 既有契约，后续只能做兼容字段收缩或升级协议版本。
3. **正式冻结。** 五个重点审查领域、专项测试 7/7、零容忍指标 10/10、验收矩阵 36/36、Windows Electron 烟测与 Schema 81 兼容均通过。ADR-0071、机器报告、验收报告和计划书已同步为正式冻结；后续只接受兼容修复，新能力进入独立专项和新协议版本。

## 下一步边界

下一推荐工作是 LIFE v2 立项审计，而不是直接创建 Schema 82：分别审查 `structured-inner-state-v1` 与 `short-memo-v1` 的真实陪伴价值、与现有状态/记忆/目标对象的重叠、单写者、隐私、TTL、迁移、回滚和产品入口，再决定是否立项及顺序。
