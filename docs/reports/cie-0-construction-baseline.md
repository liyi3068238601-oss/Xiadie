# CIE.0 ConstructionBaseline 与交互固定评测集

- predecessor：KIG PR #4 merge `b436e9f8876f8926ac90df3562edbeef3f085413`。
- Schema：80；CIE.0 不占迁移，81 仅为后续暂定候选。
- 冻结测试基线：后端 `2560 passed, 1 warning`；前端 `52 passed`；Vite 190 modules；Electron contract 3 项。
- 合成固定集：连续轮数 [5, 20, 100, 500]，共 625 条消息；打断 20、附件 20、节奏 20、第三方贡献 20 条。
- fixture SHA-256：`028d97b1106ef9366e094cf2eab8d5724323c5b1c56026d9eb1a2a2a24ec811f`；真实用户数据 0；Provider 调用 0。

## 当前基线指标

- 串行 fallback 发送成功率：100.00%（合成契约，不代表网络可用率）。
- 首 token 延迟：P50 1165.367 ms，P95 3241.132 ms，3 次；`deepseek/deepseek-v4-flash` 直连合成短提示，不经过聊天持久化。
- 活动生成取消支持率：0.00%。
- 合成重复回复率：0.00%。
- 第三方正文泄漏率：0.00%；当前没有贡献接口，因此是 fail-closed 基线，不代表 CIE.5 已实现。
- 文本附件支持率：100.00%；原生图片支持率：0.00%。
- 当前 SSE 原文路径语义改写率：0.00%。

## 冻结 fallback 与开关

唯一开关为 `cie_enabled`，默认关闭。CIE.0 不把开关接入聊天热路径，因此关闭状态严格保持当前单消息、单生成、纯文本 SSE 与本地文本附件解析路径；开启也尚不宣称任何 CIE.1+ 能力。

## 缺口结论

消息积累、活动生成取消、原生图片、客户端节奏状态机和 `context-contribution-v1` 均为未实现。上述 0% 是施工输入，不是质量失败；任一后续能力仍必须在失败时回到本报告冻结的 fallback。

## 回滚

CIE.0 只新增合成 fixture、离线报告、默认关闭的单一设置模块、测试和文档；无 Schema 迁移、无聊天热路径改动、无用户数据写入，可整提交回滚。
